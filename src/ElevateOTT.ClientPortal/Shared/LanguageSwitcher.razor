<MudMenu DisableElevation="true" Icon="@Icons.Material.Filled.Language" Color="Color.Inherit" Direction="Direction.Bottom" OffsetY="true">
    @foreach (var item in _cultures)
    {
        <MudMenuItem OnClick="() => SetCultureInfo(item)">@item.DisplayName</MudMenuItem>
    }
</MudMenu>
<MudChip Style="color:white" Disabled="true">@(_buttonText.Split("(")[0])</MudChip>

@code {

}
